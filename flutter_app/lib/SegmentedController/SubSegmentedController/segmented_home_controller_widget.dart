import 'package:flutter/material.dart';

class HomeSegmentedController extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("home"),
    );
  }
}
